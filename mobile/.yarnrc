# Yarn configuration for React Native/Expo project
# Enable network timeout to handle large packages
network-timeout 300000

# Enable progress bar for better visibility
progress true

# Use exact versions for better reproducibility
save-exact true
