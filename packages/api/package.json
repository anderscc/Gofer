{
  "name": "@gofer/api",
  "version": "0.1.0",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "initialize-search": "ts-node scripts/initialize-search.ts",
    "deploy": "serverless deploy"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.350.0",
    "@aws-sdk/util-dynamodb": "^3.350.0",
    "@gofer/search": "workspace:*",
    "aws-lambda": "^1.0.7",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119",
    "@types/node": "^20.3.1",
    "ts-node": "^10.9.1",
    "tsup": "^7.1.0",
    "typescript": "^5.1.3"
  }
}